import { Brand } from '../interfaces/common.interfaces';
export declare type EULanguages = 'cs' | 'da' | 'nl' | 'en' | 'fi' | 'fr' | 'de' | 'it' | 'pl' | 'hu' | 'no' | 'sk' | 'es' | 'sv';
export declare const EU_LANGUAGES: EULanguages[];
export declare const DEFAULT_LANGUAGE: EULanguages;
export interface EuropeanBrandEnvironment {
    brand: Brand;
    host: string;
    baseUrl: string;
    clientId: string;
    appId: string;
    endpoints: {
        session: string;
        login: string;
        language: string;
        redirectUri: string;
        token: string;
    };
    basicToken: string;
    GCMSenderID: string;
    stamp: () => string;
}
export declare const getBrandEnvironment: (brand: Brand) => EuropeanBrandEnvironment;
