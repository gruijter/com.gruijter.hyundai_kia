// moved all the US constants to its own file, we can use this file for shared constants
import { CA_ENDPOINTS } from './constants/canada';
import { EU_ENDPOINTS } from './constants/europe';
export var ALL_ENDPOINTS = {
    CA: CA_ENDPOINTS,
    EU: EU_ENDPOINTS,
};
export var GEN2 = 2;
export var GEN1 = 1;
export var REGIONS;
(function (REGIONS) {
    REGIONS["US"] = "US";
    REGIONS["CA"] = "CA";
    REGIONS["EU"] = "EU";
})(REGIONS || (REGIONS = {}));
export var DEFAULT_VEHICLE_STATUS_OPTIONS = {
    refresh: false,
    parsed: false,
};
//# sourceMappingURL=constants.js.map